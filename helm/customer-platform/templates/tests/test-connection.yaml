apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "customer-platform.fullname" . }}-test-connection"
  labels:
    {{- include "customer-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "customer-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
